{
  "name": "dash-labs",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "zip-templates": "tsx scripts/package-templates.ts",
    "generate-index": "tsx scripts/generate-template-index.ts"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.7",
    "prettier": "^3.6.2",
    "tsx": "^4.20.3",
    "turbo": "^2.5.5",
    "typescript": "5.8.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@11.3.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "adm-zip": "^0.5.16"
  }
}
